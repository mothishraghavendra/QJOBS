# GitHub Setup Commands
# Replace YOUR_USERNAME with your actual GitHub username

# Add the GitHub remote (replace YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/quantum-job-dashboard.git

# Rename the default branch to main (GitHub standard)
git branch -M main

# Push to GitHub
git push -u origin main
